apiVersion: apps/v1
kind: Deployment
metadata:
  name: fraud-processor
  namespace: fraud-platform
  labels:
    app: fraud-processor
spec:
  replicas: 2
  selector:
    matchLabels:
      app: fraud-processor
  template:
    metadata:
      labels:
        app: fraud-processor
    spec:
      containers:
        - name: fraud-processor
          image: ghcr.io/wenzheng/h-fraud-detection-fraud-processor:latest
          ports:
            - containerPort: 8080
          envFrom:
            - configMapRef:
                name: fraud-platform-shared-config
            - configMapRef:
                name: fraud-processor-config
            - secretRef:
                name: mns-credentials
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8080
            initialDelaySeconds: 20
            periodSeconds: 15
          resources:
            requests:
              cpu: 250m
              memory: 256Mi
            limits:
              cpu: 1000m
              memory: 512Mi
