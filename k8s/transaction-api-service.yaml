apiVersion: v1
kind: Service
metadata:
  name: transaction-api
  namespace: fraud-platform
spec:
  selector:
    app: transaction-api
  ports:
    - name: http
      port: 80
      targetPort: 8080
    - name: tcp-ingest
      port: 8000
      targetPort: 8000
  type: LoadBalancer
